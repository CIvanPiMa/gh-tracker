services:
  ghs-client-dev:
    image: node:24-alpine
    working_dir: /usr/src/gh-tracker
    volumes:
      - ./:/usr/src/gh-tracker
    ports:
      - '4200:4200'
    command: sh -c  "npm install && npm run start -- --host 0.0.0.0"
